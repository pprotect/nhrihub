select.dupe_check.form-control(id='{{quarternary_key}}', value='{{agency_select_params.agency_id}}')
  option(value='0', disabled=true, selected=true) select local agency...
  | {{#local_municipalities}}
  option(value='{{id}}') {{ name }}
  | {{/local_municipalities}}

script.
  export default Ractive.extend({
    template: $TEMPLATE,
    css: $CSS,
  })
