include ../filter_controls/item_select_template.pug

script.
  import Toggle from '../filter_controls/toggle'

  export default Ractive.extend({
    template : $TEMPLATE,
    source: 'legislation_ids'
    }).extend(Toggle)

