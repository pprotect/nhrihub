select.form-control#legislation(value = '{{legislation_id}}')
  option(value='0', disabled=true, style = "color:#333") {{ t.select_legislation }}
  | {{#legislations}}
  option(value='{{id}}') {{ name }}
  | {{/legislations}}

script.
  import translations from 'translations.js'

  export default Ractive.extend({
    template : $TEMPLATE,
    data : function(){ return {
      t : translations.t('legislation_selector'),
      legislation_id: 0
    }},
  });

