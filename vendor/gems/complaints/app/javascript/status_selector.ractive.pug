include filter_select_template.pug

script.
  import Toggle from './toggle'

  export default Ractive.extend({
    template : $TEMPLATE,
    oninit: function(){console.log("init status select")},
    source: 'filter_criteria.selected_statuses'
    }).extend(Toggle)
