include item_select_template.pug

script.
  import Toggle from './toggle'

  export default Ractive.extend({
    template : $TEMPLATE,
    source: 'filter_criteria.selected_status_ids'
    }).extend(Toggle)
