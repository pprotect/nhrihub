.row("data-toggle" = "edit")
  .col-md-2
    label {{ t.agency }}
  .col-md-10
    #agencies.no_edit.in.comma-list 
      span.agency {{ agency_name }}
    .edit
      <agenciesSelector agency_id='{{agency_id}}' metro_municipalities='{{metro_municipalities}}' districts='{{districts}}' provinces='{{provinces}}' agencies='{{agencies}}' top_level_category='{{top_level_category}}' />
