.row("data-toggle" = "edit")
  .col-md-2
    label {{ t.agency }}
  .col-md-10
    #agencies.no_edit.in.comma-list 
      span.agency {{ agency_description }}
    .edit
      <agenciesSelector agency_select_params='{{agency_select_params}}' metro_municipalities='{{metro_municipalities}}' districts='{{districts}}' provinces='{{provinces}}' agencies='{{agencies}}' />
