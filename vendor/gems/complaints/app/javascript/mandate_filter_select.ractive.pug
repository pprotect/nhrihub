include filter_select_template.pug

script.
  import Toggle from './toggle'

  export default Ractive.extend({
    template : $TEMPLATE,
    source: 'filter_criteria.selected_mandate_ids'
    }).extend(Toggle)
