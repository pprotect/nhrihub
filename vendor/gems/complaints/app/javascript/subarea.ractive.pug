span.subarea {{ name }}

script.
  export default Ractive.extend({
    template : $TEMPLATE,
    computed : {
      name : function(){
        return _(this.get('subareas')).findWhere({id : this.get('id')}).name
        }
      }
    })
