script.
  import FilterSelect from './filter_select.ractive.pug'

  export default FilterSelect.extend({
    computed: {
      collection: function() {
        return "area_ids";
      }
    }
  });

